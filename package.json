{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "client": "npm --prefix client run dev",
    "api": "npm --prefix api run dev",
    "dev": "concurrently \"npm run client\" \"npm run api\"",
    "build": "npm --prefix client run build && npm --prefix api run build"
  },
  "keywords": [],
  "author": "Rossignol Alex",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
